# Everything the app maintains for itself: the browser profile holding the
# Facebook login, the cumulative database, saved searches, email config, your own 
# cities, the scheduler's lock and log, and the Windows .ico built from the
# drawing in src/ui/. See src/paths.py.
.state/

# One folder per run. Not under .state/ because this is the one the galleries
# live in, and it's meant to be opened.
runs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
# OS / editor cruft
.DS_Store
.idea/
.vscode/
